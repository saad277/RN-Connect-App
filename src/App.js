import React from "react";
import { View, Text } from "react-native";

const App = (props) => {
    return (
        <View>
            <Text>App</Text>
        </View>
    );
};

export default App;
